program: main.py
method: bayes
metric:
  name: Chunk loss
  goal: minimize
parameters:
  learning_rate:
    min: 1e-05
    max: 0.01
  momentum:
    min: 0.0
    max: 5.0
  weight_decay:
    min: 1e-05
    max: 0.01
  gradient_accumulation_steps:
    min: 4
    max: 64

  
