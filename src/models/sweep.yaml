project: sweep-abstractive-summarization
entity: mikkelfo
program: src/models/sweep.py
method: bayes
metric:
  name: Epoch validation loss
  goal: minimize
parameters:
  learning_rate:
    min: 0.00001
    max: 0.001
  momentum:
    min: 0.0
    max: 1.0
  weight_decay:
    min: 0.00001
    max: 0.001
  gradient_accumulation_steps:
    min: 4
    max: 64

  
